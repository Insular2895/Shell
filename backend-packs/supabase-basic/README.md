# supabase-basic pack

Pour les apps simples : auth + DB + storage, pas de long jobs.

Cf `pack.yaml` pour les détails. Les modules `modules-registry/auth`, `modules-registry/billing`, `modules-registry/upload` sont compatibles directement avec ce pack.

Pour les apps avec long jobs : utiliser `supabase-trigger`.
