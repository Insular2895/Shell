/**
 * tree-sitter-indexer.ts — build AST index of a repo.
 * Phase 2 — uses tree-sitter to parse TS/JS/Python and build graph.json.
 */
export async function buildIndex(_repoPath: string): Promise<void> {
  console.log('[tree-sitter-indexer] Phase 2 implementation');
}
