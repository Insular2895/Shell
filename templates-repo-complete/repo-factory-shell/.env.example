# repo-factory-shell/.env.example

# GitHub auth — pour gh-clone, gh-pr-create
GITHUB_TOKEN=ghp_xxx

# Output dir for reports (default: reports/)
FACTORY_REPORTS_DIR=reports

# Optional: bin paths (default: from $PATH)
GITLEAKS_BIN=gitleaks
SEMGREP_BIN=semgrep
OSV_BIN=osv-scanner
TRIVY_BIN=trivy
