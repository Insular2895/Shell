# App feature generation

Cf `feature-generation/README.md` pour les détails.

Pipeline :
```
Idée / URL / repo
   ↓
reference-site-analyzer (cleanroom)
   ↓
feature-generation (blueprints)
   ↓
modules-registry (modules réutilisables)
   ↓
backend-packs (pattern backend)
   ↓
template instance (ex: micro-saas-template-v2)
```

Phase 2 du plan factory.
