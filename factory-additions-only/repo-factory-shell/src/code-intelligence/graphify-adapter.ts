/**
 * graphify-adapter.ts — wrapper for Graphify if installed.
 * Phase 3 — alternative to tree-sitter-indexer.
 */
export async function runGraphify(_repoPath: string): Promise<unknown> {
  console.log('[graphify] Phase 3 implementation');
  return null;
}
