# approved/

Workflows validés, versionnés, déployables en production.

Format : `<workflow-name>-<semver>.json` (ex: `lead-import-1.0.0.json`).

Toute modification :
- Bump version
- Re-review obligatoire
- ADR si breaking change
