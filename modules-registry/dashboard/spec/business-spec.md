# dashboard

Layout récurrent : sidebar nav + header + content area + widgets.
Composants : StatCard, ChartCard, RecentActivityList, EmptyState.
Theme cohérent via tokens CSS centralisés.

## Statut
Spec only. Implémentation à coder phase 2-3.
