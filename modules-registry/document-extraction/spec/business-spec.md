# document-extraction — Business spec

## Objectif
Pattern feature 'engine' du template v2 : upload PDF/image/doc → engine
extrait → résultat sous forme de blocks (text, table, file).

C'est le cas d'usage principal du template micro-saas-template-v2.

